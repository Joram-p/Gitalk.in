# Gitalk — GitHub Free Login UI

GitHub Pages compatible static Login/Register/Profile UI.

GitHub Pages cannot execute PHP or provide server-side MySQL. This package therefore does not store passwords. For real production accounts, connect a free-tier authentication provider such as Supabase/Firebase, or use a backend host.

Do not store passwords in localStorage.
